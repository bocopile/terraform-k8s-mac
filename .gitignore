# Terraform state
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.backup

# Terraform dir
.terraform/
.terraform.lock.hcl

# Local kubeconfig
kubeconfig
kubeconfig.*

# Log files
*.log
*.out
*.err

# Shell script cache/output
shell/*.log

# Cloud-init
init/*.log

# macOS system files
.DS_Store

# Zip backups or generated files
*.zip

# Secrets (example)
secrets.env
.env
.env.*

# Claude Code - Environment only (keep .claude directory in git)
# The .claude directory contains project configuration and should be committed
# Only exclude sensitive environment files

# IDE / editor files
.vscode/
.idea/*
*.swp
*.swo

#join shell
./shell/join-controlplane.sh
./shell/join.sh

# .claude/** - REMOVED: We want to commit workflow documentation
# Terraform sensitive files
terraform.tfvars
terraform.tfvars.test
*.tfstate*
.terraform/
tfplan
terraform_*.log

# Generated files
hosts.generated

# JIRA scripts (may contain API keys)
create_jira_issues*.sh
create_all_jira_issues.sh

# Addon logs
addons_install.log
